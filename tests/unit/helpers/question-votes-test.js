import { questionVotes } from 'question-answers/helpers/question-votes';
import { module, test } from 'qunit';

module('Unit | Helper | question votes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionVotes([42]);
  assert.ok(result);
});
